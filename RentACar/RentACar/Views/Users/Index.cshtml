
@{
    ViewBag.Title = "Index";
}

@if(ViewBag.displayMenu == "Yes")
{
    <h2 class="firstTitle mt-3">Welcome Admin. Now you can create user Role.</h2>
    <h3>@Html.ActionLink("Manage Role", "Index", "Role")</h3>
}
else 
{
    <h2>Welcome <strong>@ViewBag.Name</strong></h2>
}